/// Register the LuwiScript standard-library built-in functions.
///
/// Currently a placeholder — once the VM supports native-function
/// callbacks this will wire up I/O, collections, math, etc.
pub fn register_stdlib() {
    // Standard library functions are registered here
}
